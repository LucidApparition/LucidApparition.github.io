name: PR Generator

on:
  push:
    branches:
      - "*"

jobs:
  Auto-PR:
    runs-on: ubuntu-latest

    steps:
      - name: Debug
        run: |
          echo "Start time: $(date)"
          echo "Debug information"
          env
          echo "End time: $(date)"
      - name: Checkout Repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GH_TOKEN }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "16"

      - name: Install Dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Check for specific conditions and label accordingly
        run: |
          changed_files=$(git --no-pager diff --name-only ${{ github.sha }})
          # Example: If the PR modifies a file in the "docs" folder, add a "Documentation" label
          if echo "$changed_files" | grep -q '^docs/'; then
            echo "Adding Documentation label"
            gh pr label add -l "Documentation"
          fi
          # Example: If the PR modifies a file in the "frontend" folder, add a "Frontend" label
          if echo "$changed_files" | grep -q '^frontend/'; then
            echo "Adding Frontend label"
            gh pr label add -l "Frontend"
          fi
          # You can add more conditions based on your project structure and requirements
          # Additional debugging output
          echo "Changed Files: $changed_files"
          echo "GitHub Ref: ${GITHUB_REF}"
          echo "GitHub SHA: ${GITHUB_SHA}"

      - name: Create Pull Request
<<<<<<< HEAD
        run: |
          response=$(curl -s -X POST \
            -H "Authorization: Bearer ${{ secrets.GH_TOKEN }}" \
<<<<<<< HEAD
            -d '{"title": "[Auto] Pull Request", "body": "Auto-generated pull request body", "head": "development", "base": "master"}' \
            "https://api.github.com/repos/LucidApparition/LucidApparition.github.io/pulls")
  
=======
            -d '{"title": "[Auto] Pull Request", "body": "Auto-generated pull request body", "head": "auto-pr-branch", "base": "master"}' \
            "https://api.github.com/repos/${{ github.repository }}/pulls")

>>>>>>> 325b5cc9349e56ffcbce0a1c8100fbc1ad2093ad
          pr_url=$(echo "$response" | jq -r .html_url)
          echo "Pull Request created: $pr_url"
=======
        uses: peter-evans/create-pull-request@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        with:
          commit-message: "Auto-generated pull request"
          title: "[Auto] Pull Request"
          body: |
            $(cat .github/prTemplate.md)
          labels: "auto-generated"
          branch: "${{ github.ref }}"
          base: "master"
>>>>>>> parent of 85a4971 (Trying REST API for auto PR and updates to formatting.yml)
